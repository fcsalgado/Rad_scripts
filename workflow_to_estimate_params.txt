1. Create using the VCF file with multiple snps per tag, randomly sample 100 datasets (one random snp per tag everytime). Use the script create_random_vcfs.sh
2. create a SFS per random VCF using the script sfs_of_randomVCFs.sh
3. Run fsc26 for each dataset with the script param_est.sh
4. After this run joana meier script to select the best runs, saved as best_runs.sh
5. in R calcukate the confidence intervals and mean
